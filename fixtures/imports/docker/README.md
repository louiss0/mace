# Docker import fixtures

- `docker-schema.mace` exports the reusable Docker Compose schema.
- `laravel-inertia.mace` imports named schema parts with `import`.
- `mern-stack.mace` imports the whole schema with `import-as`.