|===|
alias Environment: choice["dev", "prod"];

Environment environment = "prod";
boolean preferMatch = true;
boolean useFallback = null;

string fromQuestion = preferMatch ? match (environment) {
  "dev" => "development",
  "prod" => "production",
} : "fallback";

string fromColon = useFallback ? "fallback" : match (environment) {
  "dev" => "development",
  "prod" => "production",
};
|===|

[output = 'data']
{
  from_question: fromQuestion,
  from_colon: fromColon,
}
