|===|
schema Member0: {
  field0: {
    value: string,
  },
};

schema Member1: {
  field1: {
    value: string,
  },
};

schema Member2: {
  field2: {
    value: string,
  },
};

schema Member3: {
  field3: {
    value: string,
  },
};

schema Member4: {
  field4: {
    value: string,
  },
};

type EvenValue: variant[Member0, Member1, Member2, Member3];
type EvenPair0: variant[Member0, Member1];
type EvenPair1: variant[Member2, Member3];

EvenValue evenValue0 = { field0: { value: "member-0" } };
EvenValue evenValue1 = { field1: { value: "member-1" } };
EvenValue evenValue2 = { field2: { value: "member-2" } };
EvenValue evenValue3 = { field3: { value: "member-3" } };

string evenResult0 = evenValue0 is EvenPair0
  ? "pair-0"
  : evenValue0 is EvenPair1 ? "pair-1" : "unmatched";
string evenResult1 = evenValue1 is EvenPair0
  ? "pair-0"
  : evenValue1 is EvenPair1 ? "pair-1" : "unmatched";
string evenResult2 = evenValue2 is EvenPair0
  ? "pair-0"
  : evenValue2 is EvenPair1 ? "pair-1" : "unmatched";
string evenResult3 = evenValue3 is EvenPair0
  ? "pair-0"
  : evenValue3 is EvenPair1 ? "pair-1" : "unmatched";

type OddValue: variant[Member0, Member1, Member2, Member3, Member4];
type OddPair0: variant[Member0, Member1];
type OddPair1: variant[Member2, Member3];

OddValue oddValue0 = { field0: { value: "member-0" } };
OddValue oddValue1 = { field1: { value: "member-1" } };
OddValue oddValue2 = { field2: { value: "member-2" } };
OddValue oddValue3 = { field3: { value: "member-3" } };
OddValue oddValue4 = { field4: { value: "member-4" } };

string oddResult0 = oddValue0 is OddPair0
  ? "pair-0"
  : oddValue0 is OddPair1 ? "pair-1" : oddValue0.field4.value;
string oddResult1 = oddValue1 is OddPair0
  ? "pair-0"
  : oddValue1 is OddPair1 ? "pair-1" : oddValue1.field4.value;
string oddResult2 = oddValue2 is OddPair0
  ? "pair-0"
  : oddValue2 is OddPair1 ? "pair-1" : oddValue2.field4.value;
string oddResult3 = oddValue3 is OddPair0
  ? "pair-0"
  : oddValue3 is OddPair1 ? "pair-1" : oddValue3.field4.value;
string oddResult4 = oddValue4 is OddPair0
  ? "pair-0"
  : oddValue4 is OddPair1 ? "pair-1" : oddValue4.field4.value;
|===|

[output = 'data']
{
  even0: evenResult0,
  even1: evenResult1,
  even2: evenResult2,
  even3: evenResult3,
  odd0: oddResult0,
  odd1: oddResult1,
  odd2: oddResult2,
  odd3: oddResult3,
  odd4: oddResult4,
}
