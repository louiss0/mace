|===================|
schema Project: {
  name: string;
  root: string;
};
schema Workspace: {
  name: string;
  root: string;
};
|===================|

[output=schema]
{
  project: Project,
  workspace: Workspace,
}
