|===========================================|
from "./nx_inputs.mace" import-as NxInputs;
|===========================================|

[output = data, parse = NxInputs]
{
  name: project.name,
  root: project.root,
  cwd: workspace.root,
}
