|==========================================|
from 'package_json.mace' bind Package
|==========================================|

[output = 'data', parse = Package]
{
  package_name: $name,
  package_version: $version,
  package_type: $type,
}
