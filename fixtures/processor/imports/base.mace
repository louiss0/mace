|======================================================|
from './profile.mace' import Profile:UserProfile, Age;
alias Name: string;

schema User: {
  name: Name,
  age: Age,
  profile?: UserProfile,
};

schema Secret: {
  token: int,
};

|======================================================|

[output = 'schema']
{
  Name: Name,
  User: User,
}
