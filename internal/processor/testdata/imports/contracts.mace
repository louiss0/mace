|=======================================================|
type ID: string;
schema Member: { id: ID, role: string, };
schema Team: { name: string, members: array<Member>, };
string hidden_name = "ops";
|=======================================================|

[output = schema]
{
  ID: ID,
  Member: Member,
  Team: Team,
  foo: 'bar',
  bar: 'bar',
}
