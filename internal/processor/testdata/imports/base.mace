|=====================================|
from "./profile.mace" import Profile, Age;
type Name: string;

schema User: {
  name: Name,
  age: Age,
};

schema Secret: {
  token: int,
};

|=====================================|

[output = schema]
{
  Name: Name,
  User: User,
}
