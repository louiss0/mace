|===================|

from "./foo.mace"

type Name = string;


schema User = {
  name: Name;
  age: int;
};

schema Secret = {
  token: int;
};

int local = 1;

|===================|

[output = schema]
{
  Name: Name;
  User: User;
}
