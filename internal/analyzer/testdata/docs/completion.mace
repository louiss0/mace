|===|
enum Status: string {
  Active,
  Disabled,
};

schema User: {
  name: string /# The user's display name;
  status: Status /# Current account state;
};
|===|
[output = data, schema = User]
"""
# Completion Fixture
"""
{
  name:  /# Filled by completions;
  status:  /# Filled by completions;
}
